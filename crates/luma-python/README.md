# luma-python

Python bindings for the Luma parser and LUMBA value-image helpers, built with
PyO3 and maturin.

```bash
cd crates/luma-python
maturin develop
python -c "import luma_python as luma; print(luma.version())"
```
